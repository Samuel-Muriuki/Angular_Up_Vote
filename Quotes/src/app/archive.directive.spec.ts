import { ArchiveDirective } from './archive.directive';

describe('ArchiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ArchiveDirective();
    expect(directive).toBeTruthy();
  });
});
